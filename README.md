# Academia AI

An intelligent online tutor platform.

## Setup
1. Copy `.env.example` to `.env` and fill keys.
2. `docker compose up --build`

Superadmin dashboard: `/admin/stats`
Voice setup: POST to `/voice/setup`
